import React from 'react';
import { shallow } from 'enzyme';
import EventEmptyResult from '../../../js/components/Event/EventEmptyResult';

it('should work', () => {
  const eventEmptyResult = shallow(<EventEmptyResult />)
});
