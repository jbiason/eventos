---
layout: null
---
[{% for event_hash in site.data.events %}{% assign event = event_hash[1] %}
{
"name": "{{ event.name }}",
"date": "{{ event.date }}",
"time": "{{ event.time }}",
"location": "{{ event.location }}",
"address": "{{ event.address }}",
"price": "{{ event.price }}",
"url": "{{ event.url }}",
"img": "{{ event.img }}",
"description": "{{ event.description }}",
"tags": "{{ event.tags }}"
}{% unless forloop.last %},{% endunless %}{% endfor %}
]
